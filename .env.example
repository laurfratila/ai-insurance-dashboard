# Database (enable later)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_DB=insurancedb
POSTGRES_PORT=5432

# Backend
BACKEND_PORT=8000
DATABASE_URL=postgresql+psycopg2://appuser:apppass@db:5432/insurancedb

# Frontend
FRONTEND_PORT=5173

# OpenAI (for later RAG) — leave empty in example
OPENAI_API_KEY=
