export type TimeSeriesPoint = { period: string; value: number | null };
export type BreakdownItem = { key: string; value: number };
export type DemographicItem = { age_band: string; county_name: string; customers: number };
